{
	"name": "@rhenag/designtokens",
	"version": "0.0.1",
	"description": "Rhenag design tokens for @rhenag/elements",
	"main": "build/index.js",
	"files": [
	  "build",
	  "properties"
	],
	"scripts": {
	  "build": "style-dictionary build",
	  "clean": "rm -rf build",
      "prepublishOnly": "npm run build",
	  "test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Oliver Kierepka <oliver.kierepka@rhenag.de>",
	"license": "Apache-2.0",
	"devDependencies": {
	  "style-dictionary": "^3.7.0"
	}
  }
