{
	"name": "@rhenag/designtokens",
	"version": "0.0.1",
	"description": "Rhenag design tokens for @rhenag/elements based on angular material",
	"files": [
		"build",
		"properties"
	],
	"scripts": {
		"build": "node ./build.js",
		"build:watch": "npm run build -- --watch",
		"clean": "rm -rf build",
		"transform-figma-tokens": "node ./transform-figma-tokens.js",
		"test": "jest tests --coverage=false"
	},
	"author": "Oliver Kierepka <oliver.kierepka@rhenag.de>",
	"license": "Apache-2.0",
	"dependencies": {
		"style-dictionary": "^3"
	},
	"devDependencies": {
		"@types/jest": "^27.0.3",
		"chokidar-cli": "^3.0.0",
		"jest": "^27.0.6",
		"tinycolor2": "^1.4.2",
		"ts-jest": "^27.0.3"
	},
	"repository": {
		"type": "git",
		"url": "git+https://git-lima.rhenag.de/design-system/design-token-transformer.git"
	}
}
